// Встречалось ли число раньше
#include <bits/stdc++.h>

using namespace std;

int main(){
    set<int> s;
    int n;
    while(cin >> n){
        s.insert(n);
        if()
    }
    set<int> :: iterator it;
    for(it = s.begin(); it != s.end(); it++){
        if(n == *it)
    }
    
}